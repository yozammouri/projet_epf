{% extends 'base.html.twig' %}

{% block title %}{{name}}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4">Edit Student</h1>

    <div class="card p-4 shadow-sm">
        {{ form(form) }}
        
        <div class="mt-3">
            <a href="{{ path('app_etudiant') }}" class="btn btn-secondary">Back</a>
        </div>
    </div>
</div>
{% endblock %}